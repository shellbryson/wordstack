import React, { Component } from 'react';
import Title from './components/PageTitle';

export default class SprintUntil extends Component {

  render() {
    return (
      <div>
        <PageTitle title="Sprinting... (until)" />
      </div>
    );
  }
};
